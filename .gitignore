# Internal strategy / planning — never share
PLAN.md
STRATEGY-INTERNAL.md

# Data & caches — code only
data/
*.db
__pycache__/
.venv/
*.pyc
.DS_Store

# Working/raw artifacts
examples/raw/
*.cache

# Per-property working data — raw scrapes / generated output may carry owner PII.
# The sanitized judgment.json + beacon.json (parsed public structure) ARE tracked as
# worked examples; the raw Beacon page text and the regenerable artifacts are not.
properties/**/collected_data.json
properties/**/beacon_raw.json
properties/**/beacon_raw/
properties/**/analysis.json
properties/**/packet.html

# Internal working docs (never ship)
HANDOFF.md
.workflows/
